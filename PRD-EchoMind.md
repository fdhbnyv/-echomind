# 声念 EchoMind — 产品需求文档 (PRD)

> **将碎片思维，一语成章。**

---

## 1. 文档信息

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.1 |
| 创建日期 | 2026-07-03 |
| 最后更新 | 2026-07-03 |
| 文档状态 | Draft → Review |
| 作者 | 何天鸿 (产品设计师) |
| 对应模板文件 | `templates/daily-review.yaml`、`templates/quick-idea.yaml`、`templates/meeting-notes.yaml` |
| 产品名称 | 声念 (EchoMind) |
| 产品类型 | 语音交互式个人效能工具 |
| 首发平台 | Android 移动端 (原生 App) |

---

## 2. 产品概述

### 2.1 一句话定位

**对着手机说出一天的杂乱想法，AI 自动整理成结构化的 Notion / Obsidian 笔记或日程表。**

### 2.2 产品愿景

成为知识工作者和碎片思考者的「外脑」——让每一段闪念都不被浪费，让结构化不再是负担而是本能。

### 2.3 核心价值主张

| 用户痛点 | EchoMind 解法 |
|---------|-------------|
| 碎片想法转瞬即逝，来不及打字 | 语音即输入，零门槛捕获 |
| 录音/备忘录是无结构的长文本，事后懒得整理 | AI 自动提取主题、行动项、日程、关键观点 |
| 记录散落在不同 App 里 | 一键同步到 Notion、Obsidian、系统日历 |
| 想做结构化笔记但不知道从哪开始 | 丰富模板库，语音直出结构化内容 |
| ADHD/执行功能障碍：打开App→打字→整理三步中断 | 一句话完成全流程，认知负荷降至最低 |

### 2.4 产品原则

1. **捕获第一**：任何导致用户放弃记录的设计都是失败的设计。追求零步录音。
2. **AI 做减法，不做加法**：AI 的价值不是生成更多内容，而是把混乱变整齐。
3. **用户拥有数据**：所有笔记直接写入用户自己的 Notion/Obsidian，不做数据绑架。
4. **模板即产品**：模板的质量和深度是核心竞争力，技术不构成壁垒。
5. **单一入口，多重出口**：一个语音入口，输出到多种笔记生态和日历系统。

---

## 3. 市场与用户分析

### 3.1 目标用户画像（综合覆盖）

#### 画像A：知识工作者 — 核心付费用户

| 维度 | 描述 |
|------|------|
| 人群 | 产品经理、咨询顾问、研究员、律师、管理者、自由职业者 |
| 年龄 | 25–45 岁 |
| 技术素养 | 中高，已有 Notion / Obsidian / 飞书 使用习惯 |
| 核心场景 | 会后复盘、读完文章/播客的记录、睡前总结一天、通勤时灵感记录 |
| 痛点 | "一天下来脑子里一堆东西，坐到电脑前又忘了" |
| 付费意愿 | 高。时间成本 > $5/mo。愿意为效率工具付费 |
| 单用户价值 | $5–$10/mo |
| 预估规模 | 中国 300–500 万 + 全球 800–1200 万 |

#### 画像B：ADHD / 神经多样性群体 — 高粘性种子用户

| 维度 | 描述 |
|------|------|
| 人群 | 确诊或自诊 ADHD 的成年人、执行功能障碍者、焦虑/抑郁伴随注意力问题人群 |
| 年龄 | 20–40 岁 |
| 技术素养 | 中高（自己搜索工具），对工具极其挑剔 |
| 核心场景 | "脑子里同时有 5 条思路，打开 Notion 就忘了要写什么" — 需要零门槛的外化工具 |
| 痛点 | 传统笔记工具的多步骤流程是执行功能的灾难；需要「一句话完成」的体验 |
| 付费意愿 | 极高。找到趁手工具后忠诚度极高，自发传播力强 |
| 单用户价值 | $5–$10/mo |
| 预估规模 | 中国 50–100 万（活跃在 ADHD 社区）+ 全球 200–400 万 |
| 特殊需求 | 极简 UI、零步骤录音、即时反馈、无干扰设计、暗色模式为刚需 |

#### 画像C：内容创作者 — 规模化用户

| 维度 | 描述 |
|------|------|
| 人群 | 短视频博主、播客主、公众号作者、知乎答主、B站UP主 |
| 年龄 | 20–35 岁 |
| 技术素养 | 中，使用剪辑/笔记工具 |
| 核心场景 | 录制前打腹稿、日常积累口播素材、将聊天灵感转成选题库 |
| 痛点 | "随口说的话比写出来的好，但说完就没了" |
| 付费意愿 | 中低。有替代方案（自录、听写），转化需要教育 |
| 单用户价值 | $5/mo |
| 预估规模 | 中国 500–800 万 |

### 3.2 竞品分析

| 竞品 | 覆盖场景 | 结构化能力 | 捕获便捷度 | 多端同步 | 弱点 |
|------|---------|-----------|-----------|---------|------|
| **系统语音备忘录** | 录音 | 无 | 高 | 无 | 纯录音，无结构化 |
| **Otter.ai** | 会议转录 | 摘要级 | 中 | 有限 | 聚焦会议，不处理碎片思维 |
| **AudioPen.ai** | 语音→AI摘要 | 中等 | 高 | 无多端输出 | 最接近竞品，但模板浅、无多端输出 |
| **Voicenotes.com** | 语音笔记+AI问答 | 弱 | 高 | 封闭生态 | 结构化弱，数据锁在自家平台 |
| **Notion AI** | 笔记辅助 | 高 | 低 | 原生 | 需打开App→找页面→输入，三步门槛 |
| **飞书妙记** | 会议转录 | 中 | 中 | 飞书生态内 | 仅飞书会议场景 |
| **ChatGPT / Claude App** | 通用语音对话 | 用户自控（说"帮我总结"） | 高（App 内语音模式） | 封闭 | 用户需自己描述格式，每次对话重新描述，无记忆、无模板、无多端分发 |
| **EchoMind (本品)** | 碎片思维整理 | 高（模板驱动） | 极高（Widget 一键录音） | 多端（Notion/Obsidian/日历） | 需验证模板结构化质量，用户需理解「模板」概念 |

### 3.3 差异化定位

```
核心差异 = 即时语音捕获 × 深度结构化模板 × 多端生态输出

竞品最多做到「语音→一段总结」。
EchoMind 做到「语音→按模板结构化→自动分发到用户的笔记生态」。

这不是一个语音转文字工具，这是一个「思维外化即整理」的工具。
```

### 3.4 核心差异化验证问题

> **最大的竞争盲区：用户为什么不直接用 ChatGPT App 语音模式？**

ChatGPT 的语音模式门槛更低——用户只需说"帮我总结成日报"，不需要理解"模板"是什么。

EchoMind 必须证明：**模板不是门槛，而是加速器。**

| 维度 | ChatGPT 语音 | EchoMind |
|------|-------------|----------|
| 学习成本 | 零——用户用自然语言提需求 | 有——用户需要先选择一个模板 |
| 输出一致性 | 每次对话独立，格式不固定 | 同一模板输出格式稳定，适合批量归档 |
| 多端分发 | 需手动复制粘贴到笔记工具 | 自动写入 Notion / Obsidian / 日历 |
| 速度 | 需多轮对话（用户→AI 回复→再调整） | 一句话说完，一步到位 |
| 离线能力 | 无 | 离线队列（本地录音，联网后处理） |

**EchoMind 的赢面**：让用户觉得"用 EchoMind 比跟 ChatGPT 说省了 3 轮对话+复制粘贴"。这是模板+一键分发的真实价值。

---

## 4. 用户场景与需求

### 4.1 核心使用场景 (Job Stories)

| ID | 场景 | 场景描述 | 优先级 |
|----|------|---------|--------|
| JS-01 | 每日复盘 | 睡前对着手机说几句今天做了什么、明天计划，自动生成结构化日复盘，写入 Notion 日记数据库 | P0 |
| JS-02 | 碎片想法箱 | 通勤/走路时想到一个点子，掏出手机说一句话，自动归类到"想法箱"，附时间戳和标签 | P0 |
| JS-03 | 会议复盘 | 会后快速口述会议要点和行动项，AI 提取成结构化会议纪要，同步到团队知识库 | P0 |
| JS-04 | 周报生成 | 一周内所有碎片总结，周末语音口述"本周回顾"，AI 整合历史数据生成周报 | P1 |
| JS-05 | 阅读/播客笔记 | 听完播客，口述核心要点，AI 提取成论点+案例+收获的结构化笔记 | P1 |
| JS-06 | 日程提取 | 语音中提到"明天下午3点跟老王开会"，自动识别并创建日历项 | P1 |
| JS-07 | 选题生成 | 口述一个话题方向，AI 生成结构化的选题大纲（选题→角度→大纲→金句库） | P2 |
| JS-08 | 团队复盘 | 团队成员共享模板，各自语音复盘后统一汇总到团队 Notion | P3 |

### 4.2 用户故事 (User Stories)

| ID | 作为... | 我想要... | 以便于... | 优先级 |
|----|--------|----------|----------|--------|
| US-01 | 忙碌的产品经理 | 一天结束后对着手机说 2 分钟 | 自动生成结构化日报，第二天早上直接看 | P0 |
| US-02 | ADHD 用户 | 念头一出现就立刻说出（不经过打字） | 想法不会被遗忘，事后能回顾 | P0 |
| US-03 | 播客主 | 录节目前随口说想法 | 自动生成选题大纲和口播要点 | P1 |
| US-04 | Notion 重度用户 | 语音记录直接出现在我的 Notion 对应页面/数据库 | 不需要手动搬运 | P0 |
| US-05 | Obsidian 用户 | 语音笔记按我的模板生成 .md 文件 | 保持本地知识库的一致性 | P1 |
| US-06 | 管理者 | 看团队的 AI 整理版日报 | 不需要每个人手动写日报 | P3 |

### 4.3 需求优先级矩阵

|  | 高价值 | 低价值 |
|------|--------|--------|
| **高实现成本** | V2/V3：团队协作、Obsidian 双向同步 | 暂不做：视频转笔记、实时协作 |
| **低实现成本** | V1 核心：语音→结构化→Notion输出、3个核心模板 | 暂缓：模板市场、用户系统（MVP阶段本地存储） |

---

## 5. 产品功能规划

### 5.1 版本路线图总览

```
V1: 核心闭环 (MVP)             V1.1: 体验补完               V2: 增长与生态
─────────────────────        ─────────────────────        ─────────────────────
2026.Q3                      2026.Q4–2027.Q1              2027.Q2–Q4

一键语音录入（直调 API）       桌面 Widget                  模板创作者平台
Whisper 转写                  离线队列 (WorkManager)       模板市场（UGC）
LLM 结构化提取                 日历日程识别                  自定义模板编辑器
3 个核心模板：                 暗色模式 + ADHD 设计          Notion 深度集成
  · 每日复盘 (模板已完成)       历史记录搜索                  Obsidian 本地同步
  · 碎片想法 (模板已完成)       录音回听 + 重新转写           用户账号系统
  · 会议纪要 (模板已完成)       首次使用引导（FTUE）          付费墙 + 订阅
一键写入 Notion               模板质量迭代
                              种子用户封闭测试

**关键决策：V1 无后端，Android 客户端直调 OpenAI + Notion API**
**原因：零后端可 2-3 周交付 MVP，优先验证核心假设（结构化质量）**
**何时引入后端：达到 100 名活跃用户或需要用户系统时**

--- 验证点：结构化质量达标（零编辑率 > 60%）→ 进入 V1.1 ---
--- 验证点：D7 留存 > 30% → 进入 V2 -------------------------
```

### 5.2 V1 核心功能 (P0)

#### 5.2.1 一键语音录入

- **触发方式**：
  - 桌面小组件 (Widget)：点击即开始录音，无需打开 App（V1.1 实现）
  - 通知栏快捷入口：下拉通知栏 → 点击"开始记录"（V1.1 实现）
  - App 内主界面：大红色录音按钮，单点击开始/结束（V1 实现）
- **交互设计原则**：
  - 从「想记录」到「开始说话」不超过 1 步
  - 录音中只显示波形动画 + 时长，零干扰
  - 录音结束自动开始处理，后台静默完成
- **离线队列**：
  - 无网络时本地存储录音文件，联网后自动上传处理
  - 避免地铁/电梯等场景丢失数据

#### 5.2.2 转写与结构化引擎

- **技术栈**：
  - 语音转写：Whisper API (OpenAI) 或本地 Whisper 模型
  - 结构化提取：LLM (GPT-4 / Claude) + 结构化 prompt 工程
- **处理流程**：

```
录音 → Whisper转写(文本) → LLM分析 → 结构化输出 → 回传App预览 → 用户确认/手动编辑 → 写入目标
                          ├── 主题识别
                          ├── 行动项提取
                          ├── 日程识别 & 时间解析
                          ├── 关键观点提炼
                          └── 情绪标签
```

- **输出格式**：
  - 整理结果默认生成为 **Markdown 格式**（.md）
  - 支持标题层级（H2/H3）、加粗、列表（有序/无序）、引用等标准 Markdown 语法
  - 支持自动提取标签，以 Markdown tag 形式追加到文末
- **本地保存**：
  - 每次整理结果自动保存为 **Markdown 文件**，存储在 App 本地
  - 保存内容包含：格式化正文 + 标签 + 元数据（时间、输入方式）
  - 保存后可在历史记录中随时查阅、复制、重新同步到 Notion
- **编辑能力**：
  - 用户可在生成结果界面对任意部分进行修改、删除、重新排版
  - 编辑后自动更新本地保存的 Markdown 内容

#### 5.2.3 三大核心模板

| 模板名称 | 触发场景 | 结构化输出 | 目标输出端 | 模板文件 |
|---------|---------|-----------|-----------|---------|
| **每日复盘** | 睡前/一天结束后 | 今日成就 列表 / 未完成任务 / 明日计划 / 心情评分 / 关键反思 | Notion 日记数据库 | `templates/daily-review.yaml` |
| **碎片想法** | 随时，无固定时间 | 想法标题 / 核心内容 / 灵感来源 / 行动建议 / 关联标签 | Notion 想法箱 / Obsidian | `templates/quick-idea.yaml` |
| **会议纪要** | 会议结束后 | 会议主题 / 参与人 / 关键决策 / 行动项(负责人+DDL) / 下次会议时间 / 待办 | Notion 会议数据库 | `templates/meeting-notes.yaml` |

每个模板包含：
- `llm_prompt`：LLM 结构化提取指令，定义了字段提取规则和格式约束
- `output_schema`：输出 JSON Schema，精确到字段类型、枚举值、是否可空
- `markdown_template`：面向用户的 Markdown 渲染模板
- `notion_mapping`：字段到 Notion 数据库属性的映射规则
- `example`：一个真实输入输出的示例，用于调试和用户预览

> **实际模板文件位置**：`templates/daily-review.yaml`、`templates/quick-idea.yaml`、`templates/meeting-notes.yaml`
> 这些模板是**可执行的产品资产**——可以直接喂给 LLM 测试结构化质量。

#### 5.2.4 一键写入 Notion

- 用户首次使用完成 Notion OAuth 授权
- 自动映射：模板字段 ↔ Notion 数据库属性
- 支持的目标页面类型：
  - 指定数据库的指定视图
  - 指定父页面下的子页面
  - 日志模式：按日期自动创建/追加

### 5.3 V2 模板市场与生态 (P1)

#### 5.3.1 模板市场

```
核心逻辑：让创作者（未必是开发者）设计和出售语音模板
模板 = 结构化 prompt + 输出格式定义 + 目标端映射配置
```

- **模板包含**：
  - `prompt.yaml`：LLM 提取 prompt，定义结构化字段
  - `output.json`：输出 Schema 定义
  - `mapping.json`：Notion/Obsidian/日历字段映射规则
  - `meta.json`：模板名称、描述、图标、分类、售价
  - `example.md`：一个示例输入输出

- **模板类型示例**：
  - 专业模板：产品周报、OKR 复盘、用户访谈记录、读书笔记、学术论文阅读笔记、代码 Review 记录
  - 生活模板：旅行日记、饮食记录、情绪日记、习惯追踪
  - 创作者模板：选题策划、口播脚本、短视频分镜、播客大纲

- **模板定价**：
  - 平台提供 5 个免费模板（V1 的三个 + 阅读笔记 + 周报）
  - 创作者模板 $2–$8 一个，创作者分润 70%
  - Pro 会员 $5/mo 可无限使用全部模板

#### 5.3.2 自定义模板编辑器

- 可视化表单编辑器，无需写代码
- 拖拽定义输出字段（文本、选择、日期、标签、关系）
- 实时预览：输入示例语音文本，看 AI 会输出什么
- 导出/导入模板 JSON，支持社区分享

#### 5.3.3 Notion 深度集成

- 支持自定义 Notion 数据库的表单映射
- 单向/双向同步选项
- 支持 Relation、Rollup 等高级字段类型

#### 5.3.4 Obsidian 本地同步

- 通过本地文件夹同步或社区插件实现
- 自动按模板生成 .md 文件，遵守 Obsidian 的 YAML frontmatter 规范
- 支持 Wikilinks 和标签系统

### 5.4 V3 团队与平台 (P2–P3)

#### 5.4.1 团队协作

- 团队管理员设置统一的输出模板和 Notion 配置
- 成员各自的语音复盘自动汇总到团队知识库
- 权限控制：管理员可查看、成员仅看自己的原始录音数据

#### 5.4.2 团队 Insight 面板

- 从团队成员的结构化笔记中提取聚合洞察
- 如：本周全队最常提及的关键词、未完成任务热力图、行动项完成率

#### 5.4.3 开放能力

- RESTful API：允许第三方服务调用结构化提取能力
- Webhooks：笔记生成后可触发自定义工作流
- Zapier / n8n 集成

---

## 6. 详细功能需求

### 6.1 语音录入模块

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-AUD-001 | 一键录音 | 桌面小组件 / 通知栏快捷入口 / App 内按钮，点击即开始录音 | 从点击到开始录音 < 500ms |
| REQ-AUD-002 | 录音状态 | 录音中显示波形动画（实时音量可视化）+ 已录时长 | 美观、低功耗、不干扰 |
| REQ-AUD-003 | 结束录音 | 再次点击按钮结束，或 2 秒静音自动结束（可配置） | 自动结束准确率 > 90% |
| REQ-AUD-004 | 离线队列 | 无网络时本地存储 .m4a 或 .wav 文件，连网后自动上传 | 离线录音不丢失，恢复后 30s 内开始处理 |
| REQ-AUD-005 | 录音质量 | 支持 16kHz 单声道，满足 Whisper API 最低要求 | 安静环境转写准确率 > 95% |
| REQ-AUD-006 | 录音历史 | 本地保存所有原始录音，支持回听和手动重新转写 | 30 天内可回听，可手动触发重新转写 |
| REQ-AUD-007 | 权限管理 | 首次使用时请求麦克风权限，弹窗说明使用目的 | 不授权可进入只读模式 |

### 6.2 转写与结构化模块

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-STR-001 | 语音转文字 | 调用 Whisper API 进行高精度转写 | 清晰普通话转写准确率 > 95% |
| REQ-STR-002 | 语言支持 | V1 支持普通话和英语，自动检测 | 中英混说正确识别 |
| REQ-STR-003 | 结构化提取 | LLM 按模板 prompt 提取主题、行动项、日程、关键观点、情绪标签 | 语义级别的准确提取，关键信息召回率 > 80% |
| REQ-STR-004 | 日程识别 | 自然语言时间表达式解析（"明天下午3点"、"下周三"）为结构化时间 | 常见表达识别率 > 90% |
| REQ-STR-005 | 行动项提取 | 从语音中识别承诺性语句（"我要"、"需要做"、"TODO"）并转化为 checkbox | 提取率 > 85% |
| REQ-STR-006 | 长度适配 | 支持 30 秒碎片记录到 10 分钟长篇复盘 | 转写+结构化总耗时 < 语音时长的 50% |
| REQ-STR-007 | 结果编辑 | 生成后可文本编辑修改，支持撤销/重做 | 编辑流畅，无数据丢失 |
| REQ-STR-008 | 重新生成 | 支持以新模板对已转写文本重新结构化 | 重生成耗时 < 5s |
| REQ-STR-009 | Markdown 输出 | 整理结果生成为标准 Markdown 格式（`.md`），包含标题层级、加粗、列表等标记 | 生成内容可直接复制到支持 Markdown 的工具中正常渲染 |
| REQ-STR-010 | 本地 Markdown 保存 | 每次整理结果自动保存为 Markdown 文件在 App 本地，条目包含正文 + 标签 + 元数据 | 保存后可随时在历史记录中查看、复制、重新同步 |

### 6.3 模板系统

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-TPL-001 | 模板选择 | 录音前/后可选择结构化模板（默认记忆上次选择） | 切换模板 < 2 步操作 |
| REQ-TPL-002 | V1 核心模板 | 每日复盘、碎片想法、会议纪要 | 见 5.2.3 详细定义 |
| REQ-TPL-003 | 模板预览 | 点击模板可预览结构说明和示例输出 | 图文结合，5 秒可理解 |
| REQ-TPL-004 | 模板市场 | V2 功能，浏览/搜索/购买/安装模板 | 浏览流畅，购买一步完成 |
| REQ-TPL-005 | 自定义模板 | V2 可视化编辑器，定义输出字段和映射 | 非技术人员 10 分钟可创建模板 |
| REQ-TPL-006 | 模板版本管理 | 模板创作者可发布更新，用户可选是否更新 | 不破坏已有数据 |

### 6.4 多端同步模块

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-SYN-001 | Notion OAuth | 使用 Notion OAuth 2.0 授权，获取写入权限 | 授权流程 < 3 步 |
| REQ-SYN-002 | Notion 配置 | 首次使用选择目标数据库/页面，记忆配置 | 支持搜索选择，配置后无需再次设置 |
| REQ-SYN-003 | 写入动作 | 语音处理后，点击"同步"按钮写入 Notion | 写入成功率 > 99% |
| REQ-SYN-004 | 字段映射 | 自动映射模板字段到 Notion 数据库属性 | 属性类型匹配，不兼容时提示用户手动映射 |
| REQ-SYN-005 | 错误处理 | Notion API 不可用时本地排队，网络恢复后重试 | 最多重试 3 次，失败后通知用户 |
| REQ-SYN-006 | Obsidian 同步 | V2 支持，通过文件夹同步或插件实现 | .md 文件格式完美兼容 Obsidian |
| REQ-SYN-007 | 日历同步 | 识别日程后支持添加到 Google Calendar / 系统日历 | 日历同步可选开关 |

### 6.5 用户系统 (V2)

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-USR-001 | 注册登录 | 手机号 + 验证码 / 微信授权登录 | 注册流程 < 1 分钟 |
| REQ-USR-002 | 账户恢复 | 更换设备时通过手机号恢复配置和偏好 | 恢复后模板和同步配置完好 |
| REQ-USR-003 | 数据备份 | 模板配置和偏好设置云端备份 | 备份自动进行，手动可导出 |

### 6.6 付费体系 (V2)

| 需求编号 | 功能 | 详细描述 | 验收标准 |
|---------|------|---------|---------|
| REQ-PAY-001 | 免费版限制 | 每月 30 条语音、3 个模板（复盘/想法/会议）、仅 Notion 输出 | 限制清晰、不阻断核心体验 |
| REQ-PAY-002 | Pro 订阅 | $5/mo，解锁：无限语音、全部模板、自定义模板、Obsidian/日历同步、历史搜索 | 订阅一键完成 |
| REQ-PAY-003 | 模板购买 | 单独购买创作者模板 $2–$8，永久使用 | 购买后可立即使用 |
| REQ-PAY-004 | 支付渠道 | 微信支付为主，Google Play 内购为辅 | 支付成功率和体验对标微信 |
| REQ-PAY-005 | 免费试用 | 新用户 7 天 Pro 全功能免费试用 | 试用结束前 1 天推送提醒 |

### 6.7 错误处理策略

| 错误场景 | 用户感知 | 客户端行为 | 恢复策略 |
|---------|---------|-----------|---------|
| 麦克风拒绝权限 | 录音按钮置灰，弹窗引导 | 显示"需要麦克风权限才能录音"说明 | 点击"去设置"跳转系统权限页 |
| 录音写入失败（存储空间不足） | 中断录音，提示清理空间 | 保存已录部分，弹窗提示可用空间 | 引导清理后恢复 |
| Whisper API 超时（> 30s） | 显示"转写超时，请重试" | 自动重试 1 次，仍失败则提示 | 支持手动重新转写 |
| Whisper API 返回错误 | 转写失败，显示原始录音时长 | 记录错误码和上下文，方便调试 | 重试或跳过转写（仅保存录音） |
| GPT API 结构化失败 | 返回转写文本，不做结构化 | 降级显示纯转写文本 | 用户可重新选择模板再试 | 
| Notion API 限流 (429) | 静默排队，显示"同步排队中" | 指数退避重试，最多 3 次，间隔递增 | 排队中的任务显示独立状态 |
| Notion OAuth 过期 | 同步前提示重新授权 | 自动检测 token 有效性，提前引导刷新 | 轻量弹窗，不阻断浏览 |
| 网络断开 | 提示"网络已断开" | 录音正常进行，写入本地队列 | 网络恢复后自动处理队列 |
| 应用后台被系统杀死 | 无感知（WorkManager 接管） | Room 保存的录音文件在下次启动时自动恢复 | WorkManager 周期性检查 |

### 6.8 首次用户体验 (FTUE)

FTUE（First-Time User Experience）是决定留存的第一道关卡。以下是 V1 MVP 的 FTUE 设计：

**启动流程（4 步，< 60 秒完成）**：

```
第1步：欢迎页（2 屏，可跳过）
  └── "说句话，AI 帮你整理成笔记"
  └── "你的数据归你所有，直接写入你的 Notion"

第2步：麦克风权限请求
  └── 展示权限用途："我们需要麦克风来记录你的想法"
  └── 拒绝后进入只读模式（可浏览示例，但不能录音）

第3步：快速示例引导
  └── 展示一条示例录音的结构化效果（语音→结构化笔记→Notion）
  └── "点击试试看" → 进入录音界面

第4步：第一条录音
  └── 默认选中"碎片想法"模板（最小心智负担）
  └── 录音完成后显示结果预览 + "你说了……"引用
  └── 底部两个按钮：「保存到 Notion」 / 「不满意，重新说」
```

**FTUE 关键原则**：

| 原则 | 说明 |
|------|------|
| 零账号 | V1 不要求注册，不弹登录窗 |
| 见结果再承诺 | 用户看到第一条结构化结果后，才决定要不要配 Notion |
| 可跳过 | 所有引导都可跳过，核心设计是用户点一下就能录音 |
| 到价值 < 15 秒 | 从安装完成到第一次按下录音按钮，不超过 15 秒 |

**V1.1 补充**：配 Notion 的时机放在"用户完成了 3 条录音而没有同步"时，弹窗引导："要不要把你的回顾存到 Notion 里？"

## 7. Android 平台适配专项

### 7.1 平台特性利用

| Android 特性 | 应用方式 | 用户价值 |
|-------------|---------|---------|
| **桌面小组件 (Widget)** | 4x1 / 2x1 小组件，点击即录音 | 零步骤开始记录，核心差异化体验 |
| **通知栏快捷操作** | 下拉通知栏 → "开始记录"按钮 | 无需解锁也能快速访问 |
| **快捷方式 (Shortcuts)** | 长按 App 图标 → 弹出"快速复盘"/"碎片记录"入口 | 减少 2 步操作 |
| **Material You 设计** | 跟随系统动态取色 | 融入系统视觉语言 |
| **后台任务** | WorkManager 处理离线队列 + 云端同步 | 静默完成，不影响使用 |
| **边缘手势** | 可选的边缘触控启动录音（参考 Google Assistant） | 进阶用户高效触发 |

### 7.2 性能指标

| 指标 | V1 目标 | 备注 |
|------|--------|------|
| 冷启动时间 | < 1.5s | 包括从小组件点击到录音就绪 |
| 热启动时间 | < 300ms | 后台切回 |
| 录音功能启动 | < 500ms | 点击到开始录音 |
| 内存占用 | < 150MB | 包括后台服务 |
| 安装包大小 | < 50MB | 不含本地 Whisper 模型 |
| 电池消耗 | 每小时录音 < 3% | 5 分钟录音场景 |

### 7.3 最低系统要求

- **Android 版本**：Android 9.0 (API 28) 及以上
- **存储空间**：200MB 可用空间（含离线录音缓存）
- **麦克风**：必需
- **网络**：Wi-Fi 或移动网络（离线可用，同步需联网）

### 7.4 无障碍适配

- 支持 TalkBack 屏幕阅读器完整导航
- 语音反馈模式（适合 ADHD 用户）：每一步操作有语音播报
- 高对比度模式
- 强制暗色模式选项（ADHD 用户偏好）

### 7.5 ADHD 设计 → 实现对照清单

PRD 多处提及 ADHD 用户是高粘性种子群体，以下是将描述转化为可验证的验收标准：

| PRD 声称 | 代码/UI 落地检查 | 验收标准 |
|---------|----------------|---------|
| "极简 UI" | 录音界面 → 确认是否仅 1 个核心按钮 + 1 个模板选择器 | 首屏视觉元素不超过 5 个 |
| "零步骤录音" | Widget 点击即录音，App 内默认打开就是录音就绪状态 | 从"想记录"到"开始说话"≤ 1 次点击 |
| "无干扰设计" | 录音中没有模态弹窗、没有广告、没有多余文字 | 录音中屏幕只有波形和时长 |
| "即时反馈" | 录音结束 → 转写开始 → 结构化完成，每一步有状态指示 | 转写中显示进度动画，结构化完成后柔和振动 |
| "完成确认感" | 同步成功后有明确的可视+触觉反馈 | 同步成功显示 ✓ 动画 + 振动 |
| "暗色模式为刚需" | 跟随系统或强制暗色 | Compose 主题中 `darkColorScheme` 已配置并默认启用 |
| "语音反馈模式" | 每一步操作有可选语音播报 | TalkBack 兼容 + App 内独立语音反馈开关 |
| "不打断" | 处理过程中不弹窗询问 | 所有 API 错误走静默队列或非模态提示 |

> **开发检查项**：在 V1 MV 版提交前，以上 8 条必须逐条通过。不满足的产品描述被认定为「功能未实现」而非「设计偏好」。

---

## 8. 技术架构

### 8.1 V1 架构总览（零后端方案）

**V1 关键决策：Android 客户端直接调用第三方 API，无自建后端。**

```
┌────────────────────────────────────────────────────────────┐
│                    Android 客户端                           │
│                                                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────┐   │
│  │ 录音引擎    │  │ API 客户端  │  │ 离线队列            │   │
│  │ MediaRec   │  │ OkHttp     │  │ WorkManager + Room │   │
│  │ .mp4/.aac  │  │ direct     │  └────────────────────┘   │
│  └────────────┘  │ API calls  │                             │
│                  └─────┬──────┘                             │
│  ┌─────────────────────┴────────────────────────────────┐  │
│  │                UI (Jetpack Compose)                    │  │
│  │     录音主界面 → 结果预览/编辑 → Notion 同步设置       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────┐                   │
│  │ 本地存储 (Room DB)                     │                   │
│  │ 录音文件 / 结构化结果 / 离线队列 / 配置   │                   │
│  └──────────────────────────────────────┘                   │
└────────────────────────────────────────────────────────────┘

         │ HTTPS 直调，API Key 由 App 持有
         ▼
┌─────────────────────┐  ┌─────────────────────┐  ┌──────────────┐
│  OpenAI Whisper API  │  │  OpenAI GPT API     │  │  Notion API   │
│  转写                │  │  结构化提取          │  │  写入笔记      │
│  POST /v1/audio/     │  │  POST /v1/chat/     │  │  POST /pages  │
│  transcriptions      │  │  completions        │  │               │
└─────────────────────┘  └─────────────────────┘  └──────────────┘
```

**为什么 V1 不做后端**：

| 理由 | 说明 |
|------|------|
| 验证速度 | 2-3 周即可交付 MVP，先验证"结构化质量"这个最大风险 |
| 运维成本 | 零运维，一个 Android APK 就是一个产品 |
| 数据隐私 | 数据不入自己的服务器，减少合规负担 |
| 灵活切换 | 客户端直调 API，将来加后端时不需要改动客户端核心逻辑 |

**什么时候需要引入后端**（按优先级排序）：

| 触发条件 | 需要加的模块 |
|---------|-------------|
| 达到 100 名活跃用户 | 可选：API 代理（前端混淆 + 限流 + 用量统计） |
| 需要收费 | 用户系统 + 支付 + 用量配额 |
| V2 模板市场 | 模板存储 + 创作者管理 + 计费 |
| V2 Obsidian 同步 | 文件生成服务（如果不在客户端做） |

### 8.2 关键技术选型

| 模块 | 技术选择 | 理由 |
|------|---------|------|
| Android UI | Jetpack Compose + Material 3 | Google 官方推荐，声明式 UI |
| 小组件 | Glance (Compose for Widgets) | 生态内解决 Widget（V1.1） |
| 离线队列 | WorkManager + Room | 官方推荐任务调度方案（V1.1） |
| 语音录制 | MediaRecorder (AAC) | 稳定性、压缩比好 |
| 网络层 | OkHttp + Retrofit | Android 社区最成熟的选择 |
| Whisper 转写 | OpenAI Whisper API | 直接调用，无需自建 |
| LLM 结构化 | GPT-4o-mini | 成本可控（~$0.15/百万 token），结构化能力强 |
| 消息队列 | 无需 | V1 客户端直调，无异步队列需求 |
| 对象存储 | 无需 | V1 不留存录音文件 |

### 8.3 数据流

```
用户语音 → 本地录音 → 压缩上传 → Whisper 转写 → LLM 结构化 → 返回预览
                                            ↓
                                    用户编辑/确认
                                            ↓
                                    Notion API 写入
                                    (Obsidian 本地同步)
                                    (系统日历添加)
```

### 8.4 安全与隐私

| 维度 | 方案 |
|------|------|
| 传输安全 | HTTPS + TLS 1.3 |
| 语音数据 | 转写完成后默认删除原始音频文件（客户端本地），除非用户主动选择"保存录音" |
| 文本数据 | 仅本地存储转写文本和结构化结果（Room DB），用户可随时在 App 内批量或单条删除 |
| Notion 授权 | OAuth 2.0，仅请求最小权限（写入指定页面） |
| V1 数据模型 | 全本地存储。无用户系统，无云端同步。数据写入用户自己的 Notion，服务端不留存任何数据 |
| 隐私政策 | 首次启动时明确告知：① 录音会发送到 OpenAI 进行转写 ② 结构化使用 OpenAI GPT ③ 写入 Notion 需授权 ④ 用户可随时删除所有本地数据 |
| API Key 安全 | 嵌入 App 的 API Key 通过常规混淆保护；正式版可考虑远端配置下发（V1.1 或引入代理后端后实现） |

#### 数据删除流程

V1 用户数据全部存储在本地 Android App 沙盒中，删除策略：

| 数据类别 | 存储位置 | 自动删除策略 | 用户手动删除 |
|---------|---------|-------------|-------------|
| 原始录音文件 | App 内部存储 (`.mp4`) | 转写成功后立即删除（默认）/ 保留 30 天（用户开启"保存录音"） | 单条删除 / 批量清空 |
| 整理结果（Markdown） | App 内部存储（`.md` 文件）/ Room DB 元数据索引 | 用户手动删除 | 单条删除 / 批量清空 / 清除全部数据 |
| 转写文本（缓存） | Room DB | 用户手动删除 | 单条删除 / 批量清空 / 清除全部数据 |
| Notion OAuth Token | EncryptedSharedPreferences | App 卸载时自动清除 | 设置页 → 断开 Notion 连接 |
| App 配置/偏好 | DataStore | App 卸载时自动清除 | 设置页 → 重置全部设置 |

> **完整的"删除全部数据"路径**：设置 → 数据管理 → "清除所有本地数据"（一键操作，不可撤销）

---

## 9. 用户体验设计要点

### 9.1 核心界面

| 界面 | 功能 | 设计要点 |
|------|------|---------|
| 录音主界面 | 大大的录音按钮 + 模板选择 | 极简，80% 空间给操作区 |
| 结果预览页 | 展示结构化笔记，可编辑 | 卡片式分段展示，每段可展开编辑 |
| 同步设置页 | 配置 Notion/Obsidian/日历 | 一目了然的开关列表 |
| 历史列表 | 按日期排列的所有记录 | 支持搜索、按模板过滤 |
| 模板浏览页 | 模板市场 / 我的模板 | 瀑布流或卡片列表 |

### 9.2 情绪化设计

- 录音按钮带微呼吸动画（引导用户"开始说吧"）
- 处理完成后有柔和的通知振动 + 视觉完成效果
- 生成结果的第一行带用户原话引用（"你说……"），增强「被听到」的感觉
- ADHD 友好：完成后的确认感很重要——每次同步成功有清晰的成功状态提示

---

## 10. 数据指标与成功标准

### 10.1 V1 MVP 核心指标

| 指标 | 目标值 | 失败阈值 | 测量方式 | 说明 |
|------|-------|---------|---------|------|
| 首次录音完成率 | > 80% | < 60% | 完成首次录音+生成笔记的用户 / 首次启动用户 | 引导流程是否有效 |
| 零编辑直接同步率 | > 60% | < 30% | 生成后未编辑直接同步的次数 / 总同步次数 | **核心质量指标**：AI 结构化质量是否够好 |
| 编辑后提交率 | > 90% | < 70% | 编辑后同步 / 编辑后放弃 | 编辑体验是否流畅，还是用户改到放弃 |
| D1 留存率 | > 50% | < 30% | 次日回访用户 / 首日活跃用户 | 产品是否值得第二天打开 |
| D7 留存率 | > 30% | < 15% | 第 7 天回访用户 / 首日活跃用户 | 是否形成习惯 |
| 平均每日录音数 | > 1.5 条 | < 0.5 条 | 总录音数 / 活跃用户天数 | 使用频率是否达标 |
| 首条到同步完成耗时 | < 2 分钟 | > 5 分钟 | 从首次按下录音到第一条数据写入 Notion | 端到端体验速度 |
| 主动付费意愿 | 5 个用户问付费 | < 0 | 用户主动提出的付费请求 | 需求紧迫度信号 |

> **关键修正**：v1.0 版本的「编辑后提交率 > 70%」替换为「零编辑直接同步率 > 60%」。前者接受 30% 用户流失，后者倒逼 AI 质量提升。

### 10.2 V2 商业化指标

| 指标 | 目标值 | 说明 |
|------|-------|------|
| 免费→付费转化率 | > 5% | 7天试用→首次付费 |
| 月活用户数 | > 10,000 | V2 上线后 3 个月 |
| 付费用户 LTV | > $60 | 年均订阅 12 个月 |
| 模板市场活跃创作者 | > 50 | V2 上线 6 个月 |
| 月收入 | > $5,000 | V2 上线 6 个月 |

---

## 11. 风险与对策

| 风险 | 等级 | 影响 | 对策 |
|------|------|------|------|
| **Notion 自建语音功能** | 高 | 核心差异化消失 | 1) 快速建立用户习惯 2) 多平台策略（Notion+Obsidian+日历） 3) 模板生态深度绑定 |
| **ChatGPT / Claude 语音模式** | 高 | 用户直接用通用 AI 替代 EchoMind | 1) 差异化定位见 3.4 2) 模板的「一键输出一致性」是核心优势 3) 验证结构化质量远超通用 AI 才能 justify 下载 App |
| **结构提取质量不稳定** | 高 | 用户不信任 AI 输出 | 1) 完善的编辑能力 2) 用户反馈循环优化 prompt 3) A/B 测试不同 LLM |
| **用户习惯难以培养** | 中 | 留存低 | 1) 桌面小组件降低使用门槛 2) 推送提醒培养习惯 3) 7 天连续打卡激励 |
| **API 成本失控** | 中 | 免费用户亏损 | 1) 免费版限制条数 2) 用 GPT-4o-mini 控制成本 3) 探索本地 Whisper 模型 |
| **隐私担忧** | 中 | 部分用户不敢用 | 1) V1 无后端，数据不入自己服务器 2) 清晰的隐私说明 3) 数据流向透明化 |
| **竞品抄袭** | 低-中 | 巨头入场 | 1) 模板 UGC 生态是护城河 2) 用户习惯 + 数据迁移成本 3) 聚焦细分人群做深 |
| **Android 碎片化** | 低 | 兼容性问题 | 1) 仅支持 Android 9+ 2) 主流机型真机测试 3) Firebase Test Lab |
| **API Key 泄露风险** | 中 | API Key 被逆向提取滥用 | 1) 常规混淆 + 静默轮换机制 2) 客户端限流（本地队列限速）3) 正式版引入代理后端 |

---

## 12. 附录

### A. 名词定义

| 术语 | 定义 |
|------|------|
| 语音录入 | 用户口述想法的过程，记录为音频文件 |
| 转写 | 将音频转为文本的过程，使用 Whisper API |
| 结构化 | 将转写文本按模板定义的字段进行提取和归类，生成 Markdown 笔记 |
| 模板 | 一套定义了输入语料特征、结构化提取规则和输出格式的配置包 |
| 笔记生态 | 用户已在使用的主流笔记/知识管理平台（Notion/Obsidian/飞书等） |

### B. 技术基础配置

| 配置项 | 值 |
|--------|---|
| 支持语言 | 中文（普通话）、英文；V2 增加粤语、日语 |
| 录音格式 | AAC 或 WAV，16kHz 单声道 |
| 单次录音时长 | 15 秒 – 15 分钟 |
| 转写延迟 | < 语音时长（实时性感知良好） |
| 结构化延迟 | 转写完成后 < 10 秒 |
| 免费版月限制 | 30 条语音 / 月 |

### C. 参考资料

- [Whisper API 文档](https://platform.openai.com/docs/guides/speech-to-text)
- [Notion API 文档](https://developers.notion.com/)
- [AudioPen.ai](https://audiopen.ai/) — 最接近的竞品
- [Voicenotes.com](https://voicenotes.com/) — 语音笔记竞品
- [Jetpack Compose 文档](https://developer.android.com/develop/ui/compose)
- [Glance (Compose Widgets)](https://developer.android.com/develop/ui/compose/glance)

---

> **文档维护说明**：本文档应随着产品迭代持续更新。每个版本发布后一周内更新对应章节。重大变更需在团队内 Review。PRD v1.1 的改动聚焦在：补齐模板定义、修正指标、增加错误处理/FTUE/数据删除等缺失章节、明确 V1 无后端架构决策。

> **下一步行动**：
> 1. 🟢 **立即**：用 `templates/daily-review.yaml` 的 prompt + 30 条真实语音跑通一轮结构化测试，记录零编辑率
> 2. 🟢 **立即**：用测试结果修正 llm_prompt，直到零编辑率 > 60%
> 3. 🟢 **本周**：启动 Android 客户端开发（Jetpack Compose + Kotlin），V1 直调 API，无后端
> 4. 🟡 **V1 完成前**：招募 5 名种子用户（优先 ADHD 群体）进行封闭测试
> 5. 🟡 **V1 完成后**：验证 D7 留存 > 30% → 决定是否进入 V1.1（Widget + 离线队列）
